import { BaseConfigService } from '../common/base-config.service';

class ConfigService extends BaseConfigService {
  super(env: { [k: string]: string | undefined }) {}
}

const configService = new ConfigService(process.env).ensureValues([
]);

export { configService };

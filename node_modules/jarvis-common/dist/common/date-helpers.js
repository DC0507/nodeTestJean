"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isMidnight = isMidnight;
exports.addHoursToDate = addHoursToDate;
exports.formatDatePeriod = formatDatePeriod;
const moment_1 = __importDefault(require("moment"));
function isMidnight(input) {
    const date = moment_1.default.utc(input);
    return date.get('h') === 0 && date.get('m') === 0 && date.get('s') === 0;
}
function addHoursToDate(input, hoursToAdd) {
    const date = new Date(input);
    const newDate = new Date(date.getTime() + hoursToAdd * 60 * 60 * 1000);
    return newDate;
}
function formatDatePeriod(startDate, endDate) {
    const start = moment_1.default.isMoment(startDate) ? startDate : (0, moment_1.default)(startDate);
    const end = moment_1.default.isMoment(endDate) ? endDate : (0, moment_1.default)(endDate);
    const sameMonth = start.month() === end.month();
    const sameYear = start.year() === end.year();
    if (sameMonth && sameYear) {
        return `${start.format('D')} - ${end.format('D MMM')}`;
    }
    else {
        return `${start.format('D MMM')} - ${end.format('D MMM')}`;
    }
}
//# sourceMappingURL=date-helpers.js.map
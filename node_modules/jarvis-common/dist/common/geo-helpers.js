"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.calculateBoundingBox = calculateBoundingBox;
function calculateBoundingBox(lat, lon, distanceInMeters) {
    const earthRadius = 6378137;
    const latDelta = distanceInMeters / earthRadius;
    const minLat = lat - latDelta * (180 / Math.PI);
    const maxLat = lat + latDelta * (180 / Math.PI);
    const lonDelta = distanceInMeters / (earthRadius * Math.cos((Math.PI * lat) / 180));
    const minLon = lon - lonDelta * (180 / Math.PI);
    const maxLon = lon + lonDelta * (180 / Math.PI);
    return {
        minLat,
        maxLat,
        minLon,
        maxLon,
    };
}
//# sourceMappingURL=geo-helpers.js.map